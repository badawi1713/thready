import { Spinner } from "../ui";

function SplashScreen() {
    return (
        <section className="min-h-[100dvh] flex flex-col justify-center items-center container">
            <Spinner />
        </section>
    );
}

export default SplashScreen;
